package com.example.database // Debe coincidir con el paquete generado
/*
import com.example.database.MyDatabase
import com.squareup.sqldelight.db.SqlDriver

class ClientsHelper(driver: SqlDriver) {
    private val clientQueries = MyDatabase(driver).clientQueries

    // Insertar un cliente
    fun insertClient(name: String, nick: String?) {
        clientQueries.insertClient(name, nick)
    }

    // Obtener todos los clientes
    fun getAllClients() = clientQueries.selectAllClients().executeAsList()

    // Obtener cliente por ID
    fun getClientById(id: Long) = clientQueries.selectClientById(id).executeAsOneOrNull()

    // Actualizar cliente por ID
    fun updateClient(id: Long, name: String, nick: String?) {
        clientQueries.updateClientById(name, nick, id)
    }

    // Eliminar cliente por ID
    fun deleteClient(id: Long) {
        clientQueries.deleteClientById(id)
    }
}
*/