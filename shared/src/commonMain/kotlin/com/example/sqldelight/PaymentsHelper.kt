package com.example.sqldelight
/*
import com.example.database.MyDatabase
import com.squareup.sqldelight.db.SqlDriver

class PaymentsHelper(driver: SqlDriver) {
    private val paymentQueries = MyDatabase(driver).paymentQueries

    // Insertar un pago
    fun insertPayment(pay: Double, datePay: String, clientId: Long, dueid: Long) {
        paymentQueries.insertPayment(pay, datePay, clientId, dueid)
    }

    // Obtener todos los pagos
    fun getAllPayments() = paymentQueries.selectAllPayments().executeAsList()

    // Actualizar un pago por ID
    fun updatePayment(id: Long, pay: Double, datePay: String, clientId: Long, dueid: Long) {
        paymentQueries.updatePaymentById(pay, datePay, clientId, dueid, id)
    }

    // Eliminar un pago por ID
    fun deletePayment(id: Long) {
        paymentQueries.deletePaymentById(id)
    }
}
*/