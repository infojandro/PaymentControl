package com.example.sqldelight

/*
class DueHelper(driver: SqlDriver) {
    private val dueQueries = MyDatabase(driver).dueQueries

    // Insertar un due
    fun insertDue(duename: String, amount: Double) {
        dueQueries.insertDue(duename, amount)
    }

    // Obtener todos los dues
    fun getAllDues() = dueQueries.selectAllDues().executeAsList()

    // Actualizar un due por ID
    fun updateDue(id: Long, duename: String, amount: Double) {
        dueQueries.updateDueById(duename, amount, id)
    }

    // Eliminar un due por ID
    fun deleteDue(id: Long) {
        dueQueries.deleteDueById(id)
    }
}
*/